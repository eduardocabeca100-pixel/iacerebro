{
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "preview": "vite preview",
    "vercel-dev": "vercel dev --listen 5174",
    "deploy": "vercel --prod",
    "build": "vite build"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0",
    "vercel": "latest"
  }
}